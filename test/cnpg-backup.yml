apiVersion: postgresql.cnpg.io/v1
kind: Backup
metadata:
  name: backup-example
  namespace: postgresql
spec:
  cluster:
    name: postgresql
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
