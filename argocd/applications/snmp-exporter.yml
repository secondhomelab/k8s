apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: snmp-exporter
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/secondhomelab/k8s.git
    path: monitoring/snmp-exporter
    targetRevision: HEAD
  destination:
    server: https://kubernetes.default.svc
    namespace: snmp-exporter
  syncPolicy:
    automated:
      prune: false
      selfHeal: false
      enabled: true
    syncOptions:
      - CreateNamespace=true
