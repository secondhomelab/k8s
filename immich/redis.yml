apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: immich
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: redis
  template:
    metadata:
      labels:
        app.kubernetes.io/name: redis
    spec:
      containers:
        - name: redis
          image: docker.io/valkey/valkey:9
          resources: {}
---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: immich
spec:
  ports:
    - port: 6379
  selector:
    app.kubernetes.io/name: redis
  type: ClusterIP
